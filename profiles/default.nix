# Available profiles
# Import this to make profiles available, then enable one
{
  imports = [
    # Profiles are meant to be imported individually, not all at once
    # Example: imports = [ ../profiles/development.nix ];
  ];
}
